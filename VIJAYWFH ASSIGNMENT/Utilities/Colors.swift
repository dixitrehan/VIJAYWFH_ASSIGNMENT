import SwiftUI

extension Color {
    static let customAccent = Color("AccentColor")
    static let customBackground = Color("BackgroundColor")
    static let customText = Color("TextColor")
    
    // Add more custom colors as needed
    static let customPrimary = Color.blue
    static let customSecondary = Color.gray
} 